



Node* delete(int value) {
	// first find the node and then 
	// if the node has no children then simply delete it
	// if the node has one child replace the node to be deleted with the child
	// if the node has two children then 


}
